spec:
  conversion:
    strategy: Webhook
    webhook:
      clientConfig:
        service:
          name: kamaji-webhook-service
          namespace: giantswarm
          path: /convert
      conversionReviewVersions:
        - v1
